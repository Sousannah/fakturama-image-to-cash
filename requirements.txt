# --- core (required) -------------------------------------------------------
pydantic>=2.5,<3
PyYAML>=6.0
typer>=0.12
rich>=13.0
python-dotenv>=1.0

# --- Windows UI automation (required to drive Fakturama) -------------------
uiautomation>=2.0.18      # UIA client (comtypes-based)
comtypes>=1.2             # raw UIA patterns (LegacyIAccessible, Value, ...)
pywin32>=306              # window mgmt, DPI awareness, process control

# --- vision / grounding fallback -------------------------------------------
pillow>=10.0
mss>=9.0                  # fast region screenshots
opencv-python>=4.8        # icon template matching, grid line detection
numpy>=1.24

# --- extraction -------------------------------------------------------------
requests>=2.28            # Groq chat-completions (the default provider)
easyocr>=1.7              # OCR: the primary engine
# anthropic>=0.40         # optional: the Anthropic provider / vision path

# --- optional ----------------------------------------------------------------
# pytesseract>=0.3.10     # OCR fallback (needs the Tesseract binary on PATH)

# --- producing the deliverables ------------------------------------------------
python-docx>=1.1          # scripts/build_*.py write the Word documents

# --- dev ----------------------------------------------------------------------
pytest>=8.0
